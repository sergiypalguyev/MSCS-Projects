
# OPT
  Options:
      None.				[single]
      -b.
      -f.
      -l.
      -i.
# Search String
  Length of the string:

      Zero.				[single][property zeroSearchString]
      One. 				[single]
      More than one.		
      Longer than the file. 		[single][error]
  Presence of special characters:

      None.

				[single][if !zeroSearchString]
      One.				[if !zeroSearchString]				

      Many.				[if !zeroSearchString]
# Replace String
  Length of the string:

      Zero.				[single][property zeroReplaceString]
      One.				[single]			

      More than one.		
      Longer than the file.	
	[single][error]
  Presence of special characters:

      None.
				[single][if !zeroReplaceString]
      One.				[if !zeroReplaceString]				

      Many.				[if !zeroReplaceString]	
# File Array
  Count:
      Zero.				[single][property emptyFileArray]
      One.				[single]
      Many.
# File

  Size:

      Empty.				[single][if !emptyFileArray][property emptyFile]
      Not empty.
			[if !emptyFileArray]
  Number of occurrences of the string fragment in the file:

      None.				[single][if !emptyFile][property noOccurences]			
      One.				[if !emptyFile]

      Many.				[if !emptyFile]

  Number of occurrences of the string fragment in one line:

      None.				[single][if !noOccurences && !emptyFile]
      One.				[if !noOccurences && !emptyFile]
      Many.				[if !noOccurences && !emptyFile]			

  Position of the string fragment in the file:

      First line.			[single][if !noOccurences && !emptyFile]		

      Last line.			[single][if !noOccurences && !emptyFile]		

      Any.				[if !noOccurences && !emptyFile]					


# Filename

  Presence of a file corresponding to the name:

      Not present.			[single][error]		

      Present.
